# IDMP Clinical Carbon 设计系统

本文是当前 Vue 3 演示前端的界面实现基线。它借鉴 Carbon 的生产型信息架构、分层、密度和无障碍原则，但不复制 IBM 品牌、字体或组件库。现有技术栈仍为 Vue 3、JavaScript、Element Plus 与 ECharts。

## 1. 设计目标

- 让临床质量管理人员快速判断“当前结果是否可信、是否需要行动、能否继续下钻”。
- 用信息层级、细边线和留白区分内容，不用装饰性渐变、彩色泡泡或大面积阴影。
- 一个页面只突出一个主要操作；成功、警告、错误色只表达状态，不承担装饰。
- 稳定实体、版本、任务、批次、结果和数据水位在界面上保持可辨认。
- 演示数据、真实接口数据和暂不可用能力必须有清晰来源或状态说明。

## 2. Token

Token 定义于 `src/idmp/styles/index.scss`，页面优先使用语义变量，不直接依赖色值。

### 颜色层级

| 角色 | Token | 用途 |
| --- | --- | --- |
| 页面背景 | `--idmp-background` | 应用工作区 |
| 一级表面 | `--idmp-layer-01` | 表格、表单、主要面板 |
| 二级表面 | `--idmp-layer-02` | 嵌套区、工具栏、说明区 |
| 输入背景 | `--idmp-field` | 输入框和筛选器 |
| 主文字 | `--idmp-text-primary` | 标题、数据、正文 |
| 次文字 | `--idmp-text-secondary` | 说明和标签 |
| 弱文字 | `--idmp-text-helper` | 元数据、时间、占位 |
| 主边界 | `--idmp-border-strong` | 表格和区域边界 |
| 弱边界 | `--idmp-border-subtle` | 卡片和分割线 |
| 交互色 | `--idmp-interactive` | 主操作、链接、选中态 |
| 焦点色 | `--idmp-focus` | 键盘焦点 |
| 状态色 | `--idmp-support-*` | 成功、警告、错误、信息 |

### 间距与几何

- 主网格为 8px，4px 用于紧凑微调。
- 常用间距为 4、8、12、16、24、32、40、48px。
- 普通表面圆角 2px，输入和按钮 3px，浮层 4px。
- 普通内容面板无阴影；阴影仅用于 Dialog、Popover、Dropdown 等浮层。
- 侧栏 220px，顶栏 56px，桌面端最小工作宽度 1080px。

### 排版

- 系统中文字体优先；不新增字体依赖。
- 页面标题 22px/30px，区域标题 16px/24px，正文 14px/22px，元数据 12px/18px。
- 编码、版本、比例、时间和任务标识使用等宽数字。
- 避免营销式大标题和过粗字重；数据密集页以 500–650 字重建立层级。

## 3. 应用壳层

左侧导航按稳定业务域分组：

1. 总览与分析：指标总览、指标分析、预警中心。
2. 指标配置：指标目录、因子管理、场景管理、指标映射。
3. 数据治理：数据资产。
4. 计算运行：计算任务。
5. 平台管理：系统管理。

指标编辑器是指标目录的工作流子页，不作为一级菜单。顶栏只保留面包屑、全局场景、通知和当前用户。

## 4. 页面骨架

推荐顺序：

1. `PageHeader`：标题、说明、生命周期状态、版本/水位等元数据和一个主要操作。
2. 上下文或筛选条：只放影响本页数据的条件。
3. 主要内容：表格、编辑器、图表或任务详情。
4. 次级说明：数据来源、更新时间、权限或接口缺口。

列表页使用“筛选工具栏 → 表格 → 分页”的单一主要表面。避免把每个数字都变成等权统计卡。

## 5. 组件契约

### PageHeader

- `title` 必填；`eyebrow` 只表达稳定业务域。
- `status` 与 `status-tone` 表达对象生命周期，不用颜色替代文字。
- `meta` 插槽放版本、批次、水位、更新时间等可追溯信息。
- `actions` 插槽最多放一个主按钮和少量次操作。

### StatusBadge

- 状态编码集中在 `src/idmp/design/status.js`。
- 页面只传稳定编码；兼容状态应在 API 适配层归一化。
- 状态始终显示文字；颜色只作辅助。

### StatePanel

- 统一支持 `loading`、`empty`、`error`、`permission` 和 `unavailable`。
- 错误文案说明发生了什么，并提供可执行的下一步。
- 无权限状态不能暗示“没有数据”。

### IdmpChart

- 使用统一 Clinical Carbon ECharts 主题，不用装饰性渐变。
- 支持加载、空、错误、无权限、更新时间和表格替代插槽。
- 图表标题说明业务问题；图例明确颜色含义。
- 卸载时释放实例；尊重 `prefers-reduced-motion`。

## 6. 数据表

- 表头 40px，常规行 42px；长表使用 `.table-scroll`。
- 工具栏直接操作不超过五个，低频操作进入更多菜单。
- 选择后明确显示已选数量和操作范围。
- 真实列表目标为服务端分页、稳定排序、URL Query 同步和旧请求取消。
- 患者明细不可一次性加载全部数据；敏感列由后端权限决定是否返回。

## 7. 状态与来源

- 指标版本：`DRAFT`、`VALIDATING`、`VALIDATED`、`PUBLISHED`、`DISABLED`、`ARCHIVED`。
- 异步任务：`QUEUED`、`RUNNING`、`SUCCEEDED`、`PARTIAL_SUCCEEDED`、`FAILED`、`CANCELLING`、`CANCELLED`。
- 结果：`STAGING`、`READY`、`ACTIVE`、`SUPERSEDED`、`FAILED`。
- `QUEUED` 不展示虚假百分比；`FAILED` 展示可理解原因和 traceId。
- 演示数据使用“演示数据”标签；接口失败不能静默伪装成真实数据。
- 当前后端缺失的发布、导出、映射等能力必须禁用并说明，不制造假成功。

## 8. 指标编辑工作流

按生命周期组织，而不是平铺 CRUD 标签：

1. 基础与版本：稳定指标信息、草稿版本、政策与有效期。
2. 公式与因子：Formula AST 是唯一事实源，引用具体已发布因子版本。
3. 规则与场景：受控字段、操作符、覆盖优先级和下钻路径。
4. 试算与质量：异步任务、分子分母、水位、样本和 DQ 门禁。
5. 发布检查：依赖、试算 Hash、隐私、政策和乐观锁；当前无接口时只展示门禁，不提供假发布。

已发布版本只读，变化通过创建新版本完成。影响计算的草稿变化会使旧试算结论失效。

## 9. 图表与下钻

- 默认色板限制为蓝、青、紫、橙和中性灰；红黄绿仅用于异常状态。
- 比较类图表优先共用基线和直接标签，避免多余图例。
- 重要图表提供数据表替代。
- 下钻面包屑保留业务维度、过滤条件、`resultId` 和 `snapshotId`。
- 图表联动只传业务维度键，不传物理列名或 SQL。

## 10. 无障碍与响应式

- 所有交互元素使用可见的 `:focus-visible` 焦点环。
- 不只依赖颜色传递状态；图标按钮必须有可访问名称。
- 尊重 `prefers-reduced-motion: reduce`。
- 1080/1280px 下缩小工作区留白并允许表格内部滚动；1440/1920px 下限制阅读型内容宽度，数据表仍可使用全宽。
- 当前产品以桌面端为主，不在本次范围内改造成移动端应用。

## 11. 实现边界

- 不引入 Carbon Vue、TypeScript、Pinia、Axios 或新依赖。
- 不改变现有路由地址、API 资源或真实业务含义。
- 页面层不新增任意后端端点，不把演示交互描述成生产能力。
- 任何 BIGINT ID 都按不透明字符串处理。

## 12. 已知契约差异

- 本地新增指标 DOCX 使用 `/indicators`、`/indicators/create` 等示例路由；当前项目保留 `/indicator` 与 `/indicator/edit/:id?`，不在本次视觉重构中改地址。
- 本地 DOCX 的试算状态仍出现 `PENDING / SUCCESS / CANCELED`，并使用 `TRIAL_PASS` 作为版本状态；页面统一采用在线详细设计中的 `QUEUED / SUCCEEDED / CANCELLED` 与 `DRAFT / VALIDATING / VALIDATED / PUBLISHED / DISABLED / ARCHIVED`。兼容状态只能由 API 适配层归一化。
- 本地 DOCX 设计为创建指标时原子返回 `indicatorId` 与首个 `indicatorVersionId`；当前接口实现仍分成“创建稳定指标”与“创建版本”两个前端动作。界面如实展示两步现状，不将其描述为目标态原子流程。
- 在线设计包含详情、规则/场景持久化和发布端点；当前 README 与实现尚未确认这些能力，因此编辑器只展示门禁和接口缺口，不启用假发布。
