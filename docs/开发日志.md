# IDMP 前端开发日志

本文档用于记录后续以功能为目标的开发进度。每完成一个相对完整的功能，应同步记录功能范围、涉及页面、后端接口、验证方式、提交号和遗留问题，便于和 UI、后端协作。

## 开发约定

- 主开发分支：`main`
- 当前功能基线：`main` 合并 `L` 与 `feature/indicator-category-tree` 后的集成功能集
- 基线标签：`baseline-functional-dev-20260726`
- 每完成一个功能后执行：构建验证、更新本日志、提交代码、推送 GitHub。
- 提交信息建议使用：`feat:`、`fix:`、`docs:`、`style:`、`refactor:`、`test:`、`chore:`。

## 2026-08-13

### merge: 合并 L 与指标分类树功能至主分支

- 合并来源：`L`、`feature/indicator-category-tree`，目标分支：`main`。
- 完成内容：
  - 保留 `L` 的数据资产、语义字段映射、值集业务筛选、因子与指标完整编辑、场景管理、结果下钻、删除回收站等功能。
  - 合入指标分类级联树，支持搜索、清空、任意层级选择，以及数组路径、斜杠路径和旧 `categoryMain` / `categorySub` 字段回填。
  - 新建和更新指标均提交末级分类及一、二级兼容字段，避免分类树只在新建流程生效。
  - 指标目录列表与卡片视图默认每页 9 条，分页选项统一为 6/9/12 条。
  - 保留 `L` 本轮业务筛选文案、字段搜索、转换规则预览防重复请求及全流程操作文档更新。
- 冲突处理：`IndicatorEditor.vue` 同时保留 `L` 的已有指标持久化更新、下钻路径配置和分类分支的级联树逻辑；构建产物使用独立临时目录验证，未写入孤立哈希引用。
- 验证方式：
  - `pnpm.cmd test`：22 项测试全部通过。
  - `pnpm.cmd exec vite build --outDir <临时目录>`：生产构建通过。
- 遗留问题：分类树仍为前端静态配置，待后端提供分类字典接口后改为动态加载。

## 2026-08-12

### feat: 接入第21章因子与指标删除、回收站及恢复闭环

- API 文件：
  - `src/idmp/api/modules/factors.js` 新增因子删除影响检查、软删除、回收站列表、回收站详情、恢复 5 个接口封装。
  - `src/idmp/api/modules/indicators.js` 新增指标对应 5 个接口封装；删除请求保留 JSON body，恢复/删除均透传 `resourceVersion`。
- 组件与页面：
  - 新增 `src/idmp/components/ResourceDeleteDialog.vue`，统一展示 `blockers`、`automaticActions`、`warnings`，校验删除原因，并处理 `RESOURCE-DELETE-40901`。
  - 新增 `src/idmp/views/ResourceRecycleBin.vue`，通过路由元数据复用因子/指标回收站列表、详情抽屉、恢复操作和服务端分页。
  - `src/idmp/views/FactorManagement.vue`、`src/idmp/views/IndicatorManagement.vue` 增加回收站入口、删除按钮、影响检查确认和删除成功后的列表刷新。
  - 因子列表修正后端空列表错误回退演示数据的问题：请求成功即使用 live 模式，只有请求失败才显示演示目录。
- 路由文件：
  - `src/router/index.js` 新增 `/factor/recycle-bin` 和 `/indicator/recycle-bin`，共用回收站页面并设置 `FACTOR`/`INDICATOR` 资源类型元数据。
- 构建产物：
  - 更新 `dist/index.html` 及 `dist/assets/*`，包含 `ResourceDeleteDialog`、`ResourceRecycleBin`、因子管理和指标管理的新 chunk。
- 验证方式：
  - `pnpm.cmd test` 通过，8 项测试全部通过。
  - `pnpm.cmd exec vite build --outDir dist-next` 生产构建通过；随后将 `dist-next` 产物同步到 `dist`。
- 联调验收重点：删除前必须先检查影响；有 blocker 时不能删除；DELETE body 包含最新 `resourceVersion` 和 `deleteReason`；删除成功后普通列表消失并进入回收站；恢复使用回收站详情中的 `resourceVersion`。
- 遗留问题：当前项目尚未建立完整 Pinia 会话/权限 Store，删除和恢复按钮暂未按第21章 8 个权限码做前端隐藏；后端仍需按 D01-D09 场景联调验证。

### fix: 接入指标版本 drillPaths 契约并修复新增指标流程

- 指标版本创建请求统一携带 `drillPaths`，解决后端返回“创建指标版本必须指定 drillPaths”的问题；新建版本和复制已有版本均使用同一请求适配器。
- 指标编辑页增加下钻路径和最大层级配置区域，当前在后端路径列表接口尚未明确前使用可配置默认值 `ORGANIZATION → OUT_DEPT`，并在提交前阻止空路径请求。
- 编辑已有指标版本时回填后端返回的 `drillConfig`；创建版本请求写入调试面板，便于核验路径、版本和请求体。
- 新增环境配置示例 `.env.example`、指标版本请求/响应适配器和 3 项下钻配置单元测试。
- 验证：`npm.cmd test` 通过（8 项）；使用独立输出目录执行 Vite 生产构建通过。
- 提交：`772f77a fix: align indicator version drill path contract`；已推送至 GitHub `origin/ljh`。
- 服务器部署：已生成 `.deploy-dist` 构建产物；SSH 网络可达但当前环境没有 `ljh@192.168.123.14` 可用认证凭据，上传和 Nginx reload 待配置凭据后重试。
- 遗留问题：后端尚未在 0811 文档中明确已发布下钻路径列表接口；正式结果计算快照的 `drill_path_version_id` 传递仍需后端修复。

### feat: 已有指标与因子的持久化编辑

- 因子编辑：新增 `PATCH /api/v1/factors/{id}` API 封装；已有因子进入编辑页后读取因子主对象和最近可编辑版本的 DSL，支持保存名称、说明、业务分类、DSL 与 `resourceVersion`。因子编码在编辑模式下只读，避免修改稳定业务标识。
- 指标编辑：新增 `PUT /api/v1/indicators/{id}` API 封装；编辑模式下“保存基本信息”已启用，持久化名称、分类、定义、意义、统计周期、数据来源及元数据乐观锁字段。
- 指标版本：为已有指标创建草稿版本时传入 `copyFromVersionId`，保留原版本的公式定义；已发布版本的“保存公式”按钮禁用，并提示先创建草稿版本。
- 验证：执行 `pnpm.cmd test` 通过（5 项）；生产构建见本次交付验证记录。

## 2026-08-10

### feat: M0 阶段 6 权限、错误处理、稳定性与测试收尾

- 权限元数据：为数据资产、来源元数据、数据域、语义表和语义字段路由增加 `meta.permissions` 占位码；后端尚未提供完整权限集合时保留 `backend-permission-set-optional` 兼容标记。
- 错误处理：请求层统一保留 HTTP 状态、业务 code、traceId；401 清理 token 并进入登录占位流程，403/409/400/422/500 可按状态生成明确提示。
- 稳定性：继续使用 M0 页面已有请求版本号忽略过期响应，写操作沿用 loading 防重复提交；开发兼容工具仍由 `import.meta.env.DEV` 控制。
- 测试：新增 Node 内置 `node:test`，覆盖 API adapter、BIGINT ID 保真、语义字段编码/类型和级联上下文契约；执行 `pnpm.cmd test` 通过（3 项）。
- M0-A 验收说明：真实端到端链路“来源同步→源表→数据域→语义表→字段映射→默认时间→因子选择”需连接当前实现后端执行；本地无可用后端和浏览器自动化依赖，未伪造通过结果。Chrome/Edge 1366×768、1920×1080 需在联调环境补做视觉验收。
- 后端阻塞清单：完整权限集合/登录接口、CODE 值集接口、因子试算接口以及端到端测试环境尚未提供。

### feat: M0 阶段 5 数据资产与因子编辑器正式联动

- 页面入口：因子编辑器 `/factor/new`、因子详情 `/factor/:id`；元数据工作台 `/data/domains/:id`。
- 接口覆盖：因子编辑器仅通过 `GET /api/v1/meta/data-domains`、`GET /api/v1/meta/data-domains/{domainId}/semantic-tables`、`GET /api/v1/meta/data-domains/{domainId}/semantic-tables/{tableCode}/semantic-fields` 获取正式元数据，不调用扁平字段兼容接口。
- 完成内容：严格数据域→语义表→当前语义字段三级选择；DSL 写入 `primaryDomain.domainCode/semanticTableCode`，统计字段、过滤字段和分组字段限定为当前语义表编码；切换上下文清理失效引用并忽略过期响应；字段类型驱动操作符和值编辑器；敏感字段显示标记；周期参数提交前校验默认时间字段；CODE/值集无后端值集接口时不伪造选项。
- 验证方法：执行 `pnpm.cmd run build:prod`，生产构建通过。
- 遗留问题：因子编辑覆盖保存仍受现有后端流程限制；值集选择需后端提供值集接口；试算接口不在本阶段范围内。

### feat: M0 语义字段映射和默认时间字段配置

- 页面：数据域工作台 `/data/domains/:id`
- 后端接口：`GET /api/v1/meta/source-tables/{tableName}/fields`、`GET /api/v1/meta/data-domains/{domainId}/semantic-tables/{tableCode}/semantic-fields`、`POST /api/v1/meta/data-domains/{domainId}/semantic-tables/{tableCode}/semantic-fields`、`PATCH /api/v1/meta/data-domains/{domainId}/semantic-tables/{tableCode}/default-time-field`
- 完成内容：增加物理源字段和已映射语义字段双栏展示；支持源字段选择、语义编码/名称/类型/敏感标记配置；编码按大写字母、数字、下划线且最多 64 位校验；数据类型限定为 STRING、INTEGER、DECIMAL、DATE、DATETIME、BOOLEAN、CODE；保存后回读表级字段；默认时间字段仅允许当前表已映射的 DATE/DATETIME 字段；使用请求版本号避免快速切换语义表时串数据。
- 验证方式：执行 `pnpm.cmd run build:prod`；检查字段映射和默认时间字段接口调用路径。
- 遗留问题：后端语义字段响应若不返回 sourceFieldName，页面会如实显示“未返回映射来源”，不会猜测或跨表推断；本阶段不调用数据域扁平字段接口。

### feat: M0 数据域工作台和语义表管理

- 页面：数据域工作台 `/data/domains/:id`
- 后端接口：`GET /api/v1/meta/data-domains`、`GET /api/v1/meta/data-domains/{domainId}/semantic-tables`、`GET /api/v1/meta/source-tables`、`POST /api/v1/meta/data-domains/{domainId}/semantic-tables`
- 完成内容：工作台展示数据域基础信息和状态；展示语义表 ID、编码、名称、来源物理表、源对象类型、默认时间字段和状态；多表时要求显式选择；创建语义表从已同步源表下拉选择并使用正式创建接口；创建后刷新并定位新对象；使用请求版本号忽略过期语义表响应，避免快速刷新时串数据；旧 bind-domain 表单仅在开发环境显示并标记为兼容工具。
- 验证方式：执行 `pnpm.cmd run build:prod`；检查工作台、语义表创建路由及生产构建结果。
- 遗留问题：本阶段不实现语义字段编辑、默认时间字段设置、发布、停用或影响分析。

### feat: M0 数据域列表和创建流程

- 页面：数据域目录 `/data/domains`、数据域工作台占位 `/data/domains/:id`
- 后端接口：`GET /api/v1/meta/data-domains`、`POST /api/v1/meta/data-domains`
- 完成内容：新增数据域正式列表页；展示 ID、编码、名称、说明、状态和创建时间；支持编码、名称、状态筛选；使用统一 StatusBadge；提供编码格式预校验、创建确认、防重复提交、409 重复编码提示；创建成功后刷新列表并高亮新数据域；数据域行可进入工作台路由；未提供编辑、删除、发布和停用操作。
- 验证方式：执行 `pnpm.cmd run build:prod`；检查数据域列表、创建表单和工作台路由构建结果。
- 遗留问题：数据域工作台当前仅展示基础信息并提示后续语义表配置能力，未调用未纳入本阶段的接口。

### feat: M0 来源元数据管理页面

- 页面：来源元数据 `/data/sources`
- 后端接口：`POST /api/v1/meta/source-mappings/sync`、`GET /api/v1/meta/source-tables`、`GET /api/v1/meta/source-tables/{tableName}/fields`
- 完成内容：新增源表目录和物理字段查看页面；支持表名、表类型、注释筛选；同步前二次确认、防重复提交、同步结果统计和同步表清单展示；区分未登录、无权限、后端/源数据库不可达和网络错误，并展示 traceId；保留 `/data` 原数据资产入口。
- 验证方式：执行 `pnpm.cmd run build:prod`；使用真实接口验证源表列表、源字段查询和同步闭环。
- 遗留问题：本阶段不包含数据源连接配置、密码管理、采集任务和来源结构差异对比。

### refactor: M0 数据资产接口契约与代码基线整理

- 页面：数据资产、因子编辑器
- 后端接口：沿用当前 M0 已实现接口；未新增接口
- 完成内容：新增集中式 SourceTable、SourceField、DataDomain、SemanticTable、SemanticField 模型与响应适配器；数据资产页面和因子编辑器使用稳定字段名；BIGINT ID 统一按字符串处理；在 API 层标注扁平语义字段和 bind-domain 快捷接口为兼容能力；建立 `features/meta` 及其组件目录占位。
- 验证方式：执行 `pnpm.cmd run build:prod`，生产构建通过。
- 遗留问题：数据资产页面的正式来源目录、语义表创建、字段映射编辑、值集、域关系、发布/停用和影响分析仍按后续 M0 阶段实施；当前工作区原有 FactorEditor、因子 API、组件和 dist 未提交修改未作清理。

## 2026-08-11

### fix: 补齐年度周期按钮未选中状态右边框

- 为趋势分析月度/季度/年度单选按钮组增加可见溢出和首末按钮边界样式。
- 强制年度按钮在未选中状态下保留右侧边框，避免按钮组末端边界被默认相邻按钮样式覆盖或裁切。

### fix: 修正指标分析控件溢出显示

- 趋势分析周期切换：允许趋势工具栏换行，固定月度/季度/年度单选按钮不被压缩，并收紧按钮内边距，避免“年度”文字被裁切。
- 下钻分析维度选择：将上下文栏改为自适应高度并增加内边距，固定维度选择器高度，避免选择器溢出底部灰蓝色背景区域。
- 验证：复用现有测试并完成生产构建验证。

### fix: 统一下钻明细入口文案

- 将组织维度表格中的“继续下钻”改为“查看明细”，与指标分析页已有操作文案保持一致。
- 层级关系继续通过当前层标题和面包屑表达；病例层保留“病例权限”状态，不误导为已开放病例数据。

### fix: 修正组织维度下钻层级跳跃

- 问题：全院列表展示科室时，Mock 行数据错误返回 `MEDICAL_GROUP` 作为下一层，导致点击科室后直接跳过医疗组；医疗组列表和医师列表也存在同类层级语义错位。
- 修复：统一“当前列表层级”和“行点击后的下一层级”映射为 `HOSPITAL → DEPARTMENT → MEDICAL_GROUP → DOCTOR`；病例层仍保留权限占位。
- 验证：更新下钻 Mock 链路测试，确认全院、科室、医疗组三层依次返回正确的 `nextLevels`。

### fix: 修复指标分析下钻入口并统一下钻页面布局

- 页面：指标分析页“下钻分析”标签、结果下钻 `/analysis/drill`
- 完成内容：
  - 将原先固定显示“患者级下钻尚未开放”的占位标签改为可嵌入的 `DrillExplorer`，在指标分析页直接展示组织维度下钻数据。
  - 从科室排名点击“查看明细”后，直接进入所选科室的医疗组层级；保留科室业务编码和名称，不再从全院层重复展示全部科室。
  - 抽取 `src/idmp/features/analysis/DrillExplorer.vue`，由指标分析内嵌模式和独立结果页复用相同的查询、面包屑、动态表格和下一层逻辑。
  - 独立下钻页改为系统统一的“PageHeader → 紧凑上下文栏 → 面包屑 → 指标摘要条 → 主表格”结构，减少多卡片和无效留白。
  - 将病例权限提示改为医师层下钻时的轻量提示，不再在下钻页面初始状态遮挡汇总数据。
  - 时间、病种、因子和场景维度在维度选择器中明确标记为待接入，避免用户误认为已具备真实查询能力。
- 验证方式：
  - `pnpm.cmd test` 通过，共 5 项测试。
  - 使用 `pnpm.cmd exec vite build --outDir .codex-drill-dist` 完成独立目录生产构建验证。
  - `git diff --check` 无代码空白错误。
- 遗留问题：
  - 当前组织维度仍使用 Mock 数据；真实下钻接口接入后需将数据源切换为 Live。
  - 病例、原始记录、时间/病种/因子/场景真实下钻仍依赖后端接口、权限、脱敏和审计能力。

### feat: M8 结果下钻基础链路与组织维度 Mock 演示

- 页面与路由：
  - 新增结果下钻页面 `/analysis/drill`，支持从指标分析页的科室排名入口进入。
  - 支持组织维度“全院 → 科室 → 医疗组 → 医师”的逐级 Mock 浏览、面包屑返回和当前层汇总。
  - 医师层继续下钻病例时显示权限占位，不展示伪造的患者或原始记录数据。
- 前端文件：
  - `src/idmp/api/modules/drill.js`：封装 Mock/Live 可替换的结果下钻查询服务。
  - `src/idmp/api/adapters/drill.js`：统一下钻上下文、动态列、分页、下一层和权限字段，并将 ID 归一为不透明字符串。
  - `src/idmp/features/analysis/drillData.js`：提供与目标下钻响应契约同形的组织维度 Mock 数据。
  - `src/idmp/views/ResultDrill.vue`：通用下钻页面骨架，包含口径信息、下钻层级、面包屑、汇总卡、动态表格和权限状态。
  - `src/idmp/views/IndicatorAnalysis.vue`、`src/router/index.js`：接入指标分析入口和懒加载路由。
- 接口准备：
  - Live 模式预留 `POST /api/v1/analysis/results/{resultId}/drill/search`，当前默认使用 Mock，不对接口异常做静默 Mock 降级。
  - 请求结构预留 `resultId`、`snapshotId`、`currentLevel`、`parentKeys`、`filters`、分页和排序；响应消费 `context`、`breadcrumb`、`summary`、`columns`、`records`、`nextLevels`、`pageInfo` 等字段。
- 验证方式：
  - `pnpm.cmd test` 通过，共 5 项测试，新增下钻响应适配和组织层级 Mock 测试。
  - 默认 `pnpm.cmd run build:prod` 受现有 `dist/assets` 文件权限阻止；使用独立输出目录执行 `pnpm.cmd exec vite build --outDir .codex-drill-dist` 构建成功。
- 遗留问题：
  - 当前仅完成组织维度汇总级 Mock；时间、病种、因子、场景维度仍需按同一契约扩展。
  - 后端下钻搜索、患者数据范围、脱敏、访问目的、审计、血缘和导出接口尚未纳入当前实现接口清单，暂不开放病例及原始记录真实访问。

### feat: M2 场景管理核心闭环接入

- 页面与路由：
  - 新增场景目录 `/scenarios`，支持编码、名称、类型和版本状态筛选。
  - 新增场景版本编辑 `/scenarios/:scenarioId/edit`。
  - 原 `/scene` 路径保留并重定向到场景目录；主导航改用 `/scenarios`。
- 前端文件：
  - `src/idmp/api/modules/scenarios.js`：封装场景管理 11 个 REST 接口。
  - `src/idmp/api/adapters/scenario.js`：场景类型、周期、状态、指标绑定、覆盖规则和 DTO/ViewModel 适配。
  - `src/idmp/views/ScenarioList.vue`：场景分页目录、新建场景、创建新版本入口。
  - `src/idmp/views/ScenarioEditor.vue`：基本信息、指标绑定、覆盖规则、合并预览、校验诊断和发布。
  - `src/router/index.js`、`src/idmp/layout/IdmpLayout.vue`：接入路由、菜单和 `scenario:view/edit/publish` 权限元数据。
- 后端接口：
  - `GET/POST /api/v1/scenarios`
  - `GET/POST /api/v1/scenarios/{scenarioId}/versions`
  - `GET/PATCH /api/v1/scenario-versions/{versionId}`
  - `PUT /api/v1/scenario-versions/{versionId}/indicators`
  - `PUT /api/v1/scenario-versions/{versionId}/overrides`
  - `GET /api/v1/scenario-versions/{versionId}/merged-preview`
  - `POST /api/v1/scenario-versions/{versionId}/validate`
  - `POST /api/v1/scenario-versions/{versionId}/publish`
- 完成内容：
  - 新建场景后自动进入首个草稿版本编辑页。
  - 已发布场景支持基于 `copyFromVersionId` 创建草稿版本。
  - 指标绑定仅允许选择已发布指标版本，并按完整集合提交。
  - 覆盖规则支持场景全局或指定指标版本，覆盖值提交前进行 JSON 校验。
  - 保存链路按“基本信息→指标集合→覆盖集合”顺序执行，并同步后端返回的 `resourceVersion`。
  - 修复保存基本信息回填时覆盖页面尚未提交的指标和覆盖规则问题；指标绑定表格现在直接编辑实际绑定模型。
  - 合并预览、版本校验、校验诊断和发布按钮已接入。
  - 发布和创建场景使用幂等键；409 乐观锁冲突提供重新加载最新版本处理。
  - 新增 `docs/场景管理核心闭环完整操作展示样例.md`，覆盖从创建、配置、预览、校验、发布到新版本演进的完整页面验收步骤。
- 验证方式：
  - 执行 `pnpm.cmd test`，现有 3 项测试全部通过。
  - 使用独立输出目录执行 Vite 生产构建，构建成功。
- 遗留问题：
  - 当前未实现整场景复制、JSON/YAML 导入导出、政策文件关联、场景启停/归档等缺少后端接口的能力。
  - 当前计算接口仍以因子/指标版本为计算对象，场景批量计算、场景结果和场景对比暂未接入。
  - 正式联调仍需配置 `VITE_API_BASE_URL` 或由 Nginx 将 `/api/v1` 代理到后端，并使用真实账号权限验证。

## 推荐记录格式

```md
## YYYY-MM-DD

### 类型: 功能名称

- 分支：
- 提交：
- 页面：
- 后端接口：
- 完成内容：
- 验证方式：
- 遗留问题：
```

## 2026-08-12

### fix: 指标目录每页默认展示 9 条

- 分支：`feature/indicator-category-tree`
- 提交：`4389f89 fix: show nine indicator cards per page`
- 页面：指标管理
- 后端接口：无，仅调整前端分页配置。
- 完成内容：
  - 指标列表和卡片视图的默认每页数量由 8 条调整为 9 条。
  - 两种视图的分页选项统一为 6/9/12 条，卡片视图默认可完整展示 3 × 3 排列。
- 验证方式：
  - 已执行 `pnpm run build:prod`，生产构建通过。
- 遗留问题：无。

## 2026-08-11

### feat: 支持指标分类树选择

- 分支：`feature/indicator-category-tree`
- 提交：`354395e feat: support indicator category tree selection`
- 页面：新建指标 / 指标编辑
- 后端接口：
  - `POST /api/v1/indicators`
- 完成内容：
  - 将固定的一、二级分类下拉框替换为可搜索、可清空的级联分类树，并支持选择任意层级节点。
  - 新增分类路径归一化和回填兼容，支持数组路径、斜杠分隔分类以及原有 `categoryMain` / `categorySub` 字段。
  - 创建指标时同步提交分类末级值、一级分类和二级分类，保持现有接口字段兼容。
- 验证方式：
  - 已执行 `pnpm run build:prod`，生产构建通过。
- 遗留问题：
  - 分类树当前仍由前端静态维护；后端提供分类字典接口后再改为动态加载。

## 2026-07-27

### feat: 优化因子目录分页浏览交互

- 分支：`feat/indicator-readonly-detail`
- 页面：因子管理
- 后端接口：
  - `GET /api/v1/factors`
- 完成内容：
  - 因子目录参考指标目录改为分页浏览，默认每页 8 条，并支持切换 8/12/20 条。
  - 主表精简为因子编码、因子名称、业务分类、数据域、发布状态和操作，减少横向滚动依赖。
  - 增加行展开详情，将因子 ID、已发布版本 ID、聚合方式、引用次数、数据域和状态放入展开区。
- 验证方式：
  - 已执行 `pnpm.cmd run build:prod`，生产构建通过，并同步更新 `dist`。
- 遗留问题：
  - 如果后续因子字段继续增加，建议再补充“列设置”能力，让用户自主选择显示列。

### feat: 接入 0727 新增查询接口

- 分支：`feat/indicator-readonly-detail`
- 页面：指标目录 / 指标详情 / 指标编辑 / 因子管理 / 因子编辑
- 后端接口：
  - `GET /api/v1/factors`
  - `GET /api/v1/factors/{id}`
  - `GET /api/v1/factors/{id}/versions`
  - `GET /api/v1/factor-versions`
  - `GET /api/v1/factor-versions/{id}`
  - `GET /api/v1/indicators/{id}`
  - `GET /api/v1/indicators/{id}/versions`
  - `GET /api/v1/indicator-versions/{id}`
- 完成内容：
  - 补齐指标、因子 API 模块中的详情、版本列表、版本详情查询方法。
  - 指标只读详情页改为优先读取指标详情、版本列表和版本公式定义。
  - 指标编辑页在编辑已有指标时读取真实指标详情、最新版本与 Formula AST。
  - 指标公式因子库改为优先读取已发布因子版本，并将选中的分子/分母因子版本用于公式保存。
  - 因子管理页改为优先展示后端因子列表，接口不可用时回退到演示目录。
  - 因子编辑页在通过真实因子 ID 进入时读取因子详情、版本列表和最新版本 DSL。
- 验证方式：
  - 已执行 `pnpm.cmd run build:prod`，生产构建通过，并同步更新 `dist`。
- 遗留问题：
  - 指标修改、删除、发布，因子修改、新版本、删除等接口仍未在 0727 文档中声明可用，前端继续保持不可写或只读接入。

## 2026-07-26

### feat: 指标目录查看进入只读详情页

- 分支：`fix/indicator-trial-request-headers`
- 页面：指标目录 / 指标详情
- 后端接口：
  - `GET /api/v1/indicators`
- 完成内容：
  - 新增只读指标详情页 `/indicator/view/:id`，用于展示指标目录返回的基础摘要。
  - 指标目录中“查看”和指标编码入口统一进入只读详情页，草稿和已发布指标均可查看。
  - 保留“编辑”入口，草稿指标仍可进入编辑页继续配置。
  - 详情页预留版本、公式、试算记录、规则/场景和发布门禁接口接入位置。
- 验证方式：
  - 已执行 `pnpm.cmd run build:prod`，生产构建通过。

### refactor: 调整指标编辑页的新建/编辑模式与流程上下文

- 分支：`fix/indicator-trial-request-headers`
- 页面：新建指标 / 编辑指标
- 后端接口：
  - `GET /api/v1/indicators`
  - `POST /api/v1/indicators`
  - `POST /api/v1/indicators/{id}/versions`
  - `PUT /api/v1/indicator-versions/{id}/formula`
  - `POST /api/v1/indicator-versions/{id}/formula/compile`
  - `POST /api/v1/indicator-versions/{id}/trial`
- 完成内容：
  - 区分新建与编辑模式：新建保留住院死亡率演示默认值；编辑模式不再使用演示指标冒充真实数据。
  - 编辑模式先通过指标目录接口回填摘要信息，并预留详情、版本、公式和试算记录接口接入位置。
  - 在编辑模式下禁用基础信息新建保存，避免误用创建接口覆盖已有指标；允许在已有指标 ID 上创建草稿版本。
- 验证方式：
  - 已执行 `pnpm.cmd run build:prod`，生产构建通过。
- 遗留问题：
  - 后端仍需补充指标详情、版本列表、公式详情、规则/场景持久化和发布接口后，编辑模式才能完整闭环。

### fix: 修复指标试算请求头被覆盖导致的服务内部错误

- 分支：`fix/indicator-trial-request-headers`
- 页面：新建指标 / 指标编辑 / 第16章链路自检
- 后端接口：
  - `POST /api/v1/indicator-versions/{id}/trial`
  - `GET /api/v1/async-tasks/{taskId}`
  - `GET /api/v1/calc/batches/{batchId}`
  - `GET /api/v1/indicator-versions/{id}/trials/{batchId}/results`
- 完成内容：
  - 修复 `requestJson` 中 `fetch` 参数合并顺序，避免传入 `X-Idempotency-Key` 时覆盖默认 `Content-Type: application/json` 与 `Authorization`。
  - 保留试算请求的幂等键，同时确保 JSON 请求体能被后端按接口契约正确解析。
  - 优化接口错误信息中的 `traceId` 展示格式，便于联调时快速定位后端日志。
  - 重新生成生产构建 `dist`，用于上传后端服务器部署。
- 验证方式：
  - 已执行 `pnpm.cmd run build:prod`，生产构建通过。
  - 已检查 `dist/assets/request-*.js`，确认打包产物包含新的请求头合并逻辑。
- 遗留问题：
  - 部分历史页面文案和文档内容存在编码显示异常，后续建议作为独立任务统一修复。

### fix: 完善指标试算前端诊断与第16章链路自检

- 分支：`integrate/clinical-factor`
- 页面：新建指标 / 指标编辑
- 后端接口：
  - `POST /api/v1/indicators`
  - `POST /api/v1/indicators/{id}/versions`
  - `PUT /api/v1/indicator-versions/{id}/formula`
  - `POST /api/v1/indicator-versions/{id}/formula/compile`
  - `POST /api/v1/indicator-versions/{id}/trial`
  - `GET /api/v1/async-tasks/{taskId}`
  - `GET /api/v1/calc/batches/{batchId}`
  - `GET /api/v1/indicator-versions/{id}/trials/{batchId}/results`
- 完成内容：
  - 增加前端构建标识，显示 Git commit 与构建时间，用于确认服务器是否已部署最新 dist。
  - 调试面板增加 API Base、完整 URL、指标 ID、指标版本 ID、resourceVersion、分子因子版本、分母因子版本、幂等键、请求体和 traceId。
  - 增加“第16章链路自检”按钮，独立执行创建指标、创建版本、保存公式、公式校验、发起试算、轮询任务、读取结果，不依赖手工流程状态。
  - 将发起试算与任务轮询拆开记录，避免把后续轮询问题误判为 trial 接口失败。
- 验证方式：
  - 已执行 `vite build`，生产构建通过。
- 遗留问题：
  - 需要上传最新 `dist` 到服务器后，用页面显示的构建版本确认是否真正更新。

### baseline: 功能开发基线

- 分支：`integrate/clinical-factor`
- 提交：`3563881 fix: preserve backend ids in indicator workflow`
- 标签：`baseline-functional-dev-20260726`
- 页面：
  - 指标看板
  - 指标管理
  - 新建指标 / 指标编辑
  - 指标分析
  - 因子管理
  - 新增因子 / 因子编辑
  - 计算任务中心
  - 系统管理
- 后端接口：
  - `GET /api/v1/health`
  - `GET /api/v1/indicators`
  - `POST /api/v1/indicators`
  - `POST /api/v1/indicators/{id}/versions`
  - `PUT /api/v1/indicator-versions/{id}/formula`
  - `POST /api/v1/indicator-versions/{id}/formula/compile`
  - `POST /api/v1/indicator-versions/{id}/trial`
  - `GET /api/v1/indicator-versions/{id}/trials/{batchId}/results`
  - `POST /api/v1/factors`
  - `POST /api/v1/factor-versions/{id}/compile`
  - `POST /api/v1/factor-versions/{id}/trial`
  - `GET /api/v1/factor-versions/{id}/trials/{batchId}/results`
  - `POST /api/v1/factor-versions/{id}/publish`
  - `GET /api/v1/async-tasks/{taskId}`
  - `GET /api/v1/calc/batches/{batchId}`
- 完成内容：
  - 指标看板支持编辑、拖拽、添加、删除组件。
  - 指标看板组件可从指标数据选择，并支持不同展示类型。
  - 指标卡可跳转到对应指标分析页面。
  - 指标分析页面支持已有配置指标选择。
  - 指标管理页面接入后端指标列表。
  - 指标编辑流程拆为基本信息、版本、公式、校验、试算、结果读取等步骤。
  - 新建指标流程修复了后端大整数 ID 精度丢失问题。
  - 新建指标流程修复了指标 ID 与指标版本 ID 混用问题。
  - 因子管理从开发联调面板调整为正式业务页面结构。
  - 新增因子页面支持基本信息、DSL 可读配置、校验、试算、结果和发布流程。
  - 计算任务中心、系统管理等接口按优先级进行了初步接入和展示。
- 验证方式：
  - 已执行 `npm run build` / `vite build`，生产构建通过。
  - 已通过浏览器页面验证部分后端真实数据可展示。
  - 已通过住院死亡率链路验证后端可返回真实指标结果。
- 遗留问题：
  - 新建指标发起试算阶段出现“服务内部错误”，需要结合后端 `traceId` 排查异步计算链路。
  - 指标目录分类、数据来源、删除权限等仍需要后端提供更完整接口。
  - 因子列表查询和因子编辑详情接口仍需以后端实际可用接口为准继续补齐。
  - 公式可视化编辑器当前仍以住院死亡率 demo 公式为主，后续需接入动态 Formula AST 生成和保存。

### feat: 对齐 IDMP 第18章端到端页面流程

- 分支：`feat/idmp-e2e-flow-alignment`
- 页面：
  - 数据资产准备
  - 因子管理
  - 因子编辑
  - 指标管理
  - 指标详情
  - 指标编辑
  - 指标分析
  - 指标看板
- 后端接口：
  - `GET /api/v1/data-domains`
  - `GET /api/v1/data-domains/{id}/semantic-tables`
  - `GET /api/v1/data-domains/{id}/semantic-tables/{semanticTableCode}/fields`
  - `POST /api/v1/factor-versions/{id}/trial`
  - `GET /api/v1/factor-versions/{id}/trials/{batchId}/results`
  - `GET /api/v1/factor-versions/{id}`
  - `GET /api/v1/indicators/{id}`
  - `GET /api/v1/indicator-versions/{id}`
  - `PUT /api/v1/indicator-versions/{id}/formula`
  - `POST /api/v1/indicator-versions/{id}/formula/compile`
  - `POST /api/v1/indicator-versions/{id}/trial`
  - `GET /api/v1/indicator-versions/{id}/trials/{batchId}/results`
  - `POST /api/v1/indicator-versions/{id}/publish`
  - `GET /api/v1/analysis/indicators/{id}/analysis`
- 完成内容：
  - 按 `IDMP 当前实现接口文档与测试样例.md` 第18章重写 `端到端页面可见测试操作指导.md`，固定使用 `MORTALITY_V2_DEATH_20260727_1403`、`MORTALITY_V2_DISCHARGE_20260727_1403`、因子版本 `101996817981379998` / `101996817981380000` 和指标版本 `101996817981380254`。
  - 数据资产同步失败后会重新读取数据域；如果目标语义对象已存在，页面提示可继续后续步骤，避免因重复同步或后端内部错误阻断演示流程。
  - 因子编辑页支持从已有因子 ID、因子编码或因子版本 ID 加载详情、版本、DSL、语义表和语义字段。
  - 已有因子进入编辑页后，只要加载到因子版本 ID，即可直接发起试算并查看试算结果；前端不再强制要求先重新生成编译产物。
  - 指标编辑和详情流程补齐版本发布、公式回显、试算结果读取和接口参数展示，使页面流程尽量贴合后端当前已实现接口。
  - 指标分析链路支持携带 `indicatorVersionId`、统计周期和粒度读取分析结果，并用于端到端验证 `0.26%` 等后端实测值。
- 验证方式：
  - 已执行 `pnpm.cmd run build:prod`，生产构建通过。
  - 构建后同步刷新 `dist` 产物。
- 遗留问题：
  - 后端若重新生成批次，页面展示的 `taskId` / `batchId` 可能不同于接口文档样例；验收时应以同一统计窗口下的结果值和状态为准。
  - 已有因子的基础信息更新接口当前仍未接入，编辑页仅支持真实回显、试算、结果查看及已有流程内的版本操作。

### fix: 优化新建指标发布等待与重复点击问题

- 修正指标发布前的异步试算轮询：轮询窗口延长至约 56 秒，并在查询结果前校验批次是否已进入 `SUCCEEDED` / `PARTIAL_SUCCEEDED`；批次仍在运行时提示稍后查看，不再用未生成的结果触发带 `traceId` 的错误。
- 发布接口增加 60 秒请求超时和 `X-Idempotency-Key`，同一指标版本的重试复用同一个幂等键，降低网络超时后的重复发布风险。
- 发布前刷新版本状态；若第一次请求实际已经完成发布但响应超时，后续点击会识别 `PUBLISHED` 状态并直接显示成功，不再重复提交。
- 请求层增加统一超时错误信息，避免网络连接异常时按钮长期处于等待状态。
- 验证：已检查发布、试算、批次结果查询调用链；联调时继续使用页面调试面板中的 `traceId`、taskId、batchId 和最近请求步骤核对后端日志。

### fix: 修复场景版本保存的 defaultPeriodType 枚举校验

- 背景：场景管理中的“新建版本”会通过 `copyFromVersionId` 在同一场景下复制当前发布版本，生成可编辑的 `DRAFT` 版本；保存草稿时调用 `PATCH /api/v1/scenario-versions/{versionId}`。
- 问题：后端仅接受 `DAILY`、`WEEKLY`、`MONTHLY`、`QUARTERLY`、`YEARLY`、`CUSTOM`，前端此前直接回传详情中的 `defaultPeriodType`，旧数据的 `MONTH`、`月度`、大小写或空白值会触发枚举错误。
- 修改文件：
  - `src/idmp/api/adapters/scenario.js`：增加周期枚举归一化；详情回显时兼容历史值；提交 PATCH 前严格校验，未知值提示重新选择。
  - `test/scenario-adapter.test.js`：增加周期兼容、未知值拦截和 PATCH 值校验测试。
  - `package.json`：将场景适配器测试加入 `npm test`。
  - `dist/index.html`、`dist/assets/*`：基于当前工作区重新生成生产构建产物；新增/变更的带哈希资源以本次构建结果为准，部署时需整体同步 `dist`，不要只替换单个 JS 文件。
- 合并注意：源码合并优先处理 `scenario.js` 和 `package.json`；`dist` 为构建生成物，合并其他功能后应重新执行 `npm run build:prod` 再更新，避免使用旧哈希资源。
- 验证：11 个 Node 测试全部通过；Vite 生产构建通过（临时输出目录构建后已同步至 `dist`）。

## 后续执行步骤

每完成一个功能，建议按以下顺序操作：

```bash
npm run build
git status
git diff --stat
git add .
git commit -m "feat: 功能说明"
git push
```

如果功能存在后端联调内容，应在本日志中写清楚“点击哪里、输入什么、接口返回什么、页面如何证明已经接入”。
