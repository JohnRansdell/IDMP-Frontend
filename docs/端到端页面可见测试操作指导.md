# 端到端页面可见测试操作指导

本文只说明如何通过前端页面复测 IDMP 端到端链路。测试参数必须优先对齐根目录接口文档：

```text
IDMP 当前实现接口文档与测试样例.md
```

本指导以该接口文档第 18 章“住院死亡率：语义时间字段完整链路实测（包括指标分析）（2026-07-27）”为主线。除非页面必须新建对象，否则不要自行编造业务编码、字段编码、时间范围或结果预期。

## 1. 接口文档基线

环境与约束：

```text
后端环境：http://192.168.123.14:8081
业务基础路径：/api/v1
测试期间鉴权开关处于关闭状态
计算任务由 Calc Worker / PowerJob 执行
```

接口文档第 18 章实测对象：

```text
死亡患者明细数据域编码：MORTALITY_V2_DEATH_20260727_1403
死亡患者明细数据域 ID：101996817981379593
死亡患者明细源表：vmq_deathpatientdetail
死亡患者明细默认时间语义字段：DEATH_TIME
死亡患者明细物理时间字段：death_datetime

出院病案数据域编码：MORTALITY_V2_DISCHARGE_20260727_1403
出院病案数据域 ID：101996817981379594
出院病案源表：vmq_basicinformationba
出院病案默认时间语义字段：DISCHARGE_TIME
出院病案物理时间字段：out_date

死亡患者数因子版本 ID：101996817981379998
出院人次因子版本 ID：101996817981380000

住院死亡率指标编码：MORTALITY_V2_RATE_FIXED_20260727_1412
住院死亡率指标 ID：101996817981380253
住院死亡率指标版本 ID：101996817981380254
指标公式编译产物 ID：101996817981380257

测试窗口：2026-03-01T00:00:00 至 2026-04-01T00:00:00
分子试算批次 ID：101996817981380233
分母试算批次 ID：101996817981380241
指标试算批次 ID：101996817981380260

接口文档实测分子结果：40.0
接口文档实测分母结果：15647.0
接口文档实测指标结果：0.0025564
接口文档实测展示值：0.26%
```

重要口径：

```text
第 18 章因子 DSL 使用 filters: {"nodeType":"TRUE"}。
周期过滤不是前端手写 PREDICATE，而是由语义表默认时间字段自动绑定 period 参数。
编译产物中应能看到 periodBinding.mode = SEMANTIC_TABLE_DEFAULT。
```

因此，页面验收时不要把 `DEATH_DATETIME` / `OUT_DATE` 当作本次严格复测的主字段；第 18 章使用的是语义字段 `DEATH_TIME` / `DISCHARGE_TIME`。

## 2. 数据资产准备

进入页面：

```text
数据管理
```

点击：

```text
刷新数据域
```

页面预期：

```text
1. 数据域列表加载成功。
2. 列表中能看到 MORTALITY_V2_DEATH_20260727_1403。
3. 列表中能看到 MORTALITY_V2_DISCHARGE_20260727_1403。
```

分别点击两个数据域，检查下方语义字段表格。

死亡患者明细数据域预期：

```text
数据域编码：MORTALITY_V2_DEATH_20260727_1403
应能看到语义字段：DEATH_TIME
字段来源应对应或可追溯到物理字段：death_datetime
```

出院病案数据域预期：

```text
数据域编码：MORTALITY_V2_DISCHARGE_20260727_1403
应能看到语义字段：DISCHARGE_TIME
字段来源应对应或可追溯到物理字段：out_date
```

对应接口：

```text
GET /api/v1/meta/data-domains
GET /api/v1/meta/data-domains/{domainId}/semantic-tables
GET /api/v1/meta/data-domains/{domainId}/semantic-tables/{tableCode}/semantic-fields
```

### 2.1 来源映射同步说明

接口文档中 `POST /api/v1/meta/source-mappings/sync` 是远程源库技术元数据同步接口，不是第 18 章已发布实测链路的必经前置。

如果点击“同步来源映射”后出现：

```text
服务内部错误 (traceId: ...)
```

处理方式：

```text
1. 记录 traceId，供后端排查远程源库连接或同步任务日志。
2. 点击“刷新数据域”。
3. 如果两个 MORTALITY_V2_* 数据域和 DEATH_TIME / DISCHARGE_TIME 字段已存在，可以继续后续测试。
4. 如果两个 MORTALITY_V2_* 数据域不存在，不要改用 INPATIENT_* 数据自造主流程，应先按接口文档第 18 章准备数据资产或让后端排查。
```

### 2.2 不要用旧参数替代严格复测参数

以下对象属于旧链路或兼容链路，不作为第 18 章严格复测的主验收对象：

```text
INPATIENT_DEATH_RECORD
INPATIENT_DISCHARGE_RECORD
DEATH_DATETIME
OUT_DATE
```

只有在明确做“兼容旧链路验证”时，才使用上述对象。

## 3. 因子版本复测

接口文档第 18 章已经发布了两个可用因子版本：

```text
死亡患者数因子版本 ID：101996817981379998
出院人次因子版本 ID：101996817981380000
```

进入页面：

```text
因子管理
```

优先通过列表、筛选或编辑入口确认这两个版本存在并为已发布状态。

页面预期：

```text
1. 死亡患者数因子版本状态为 PUBLISHED。
2. 出院人次因子版本状态为 PUBLISHED。
3. 因子 DSL 或详情中能体现数据域分别来自 MORTALITY_V2_DEATH_20260727_1403 与 MORTALITY_V2_DISCHARGE_20260727_1403。
4. 编译产物状态为 VALID。
```

若页面支持查看编译产物，死亡患者数因子编译产物至少应符合接口文档第 18.1 节：

```text
artifactId：101996817981380001
status：VALID
parameterSchema 中包含 period，type 为 DATETIME_RANGE，systemProvided 为 true
logicalPlan.periodBinding.mode：SEMANTIC_TABLE_DEFAULT
logicalPlan.periodBinding.fieldCode：DEATH_TIME
logicalPlan.periodBinding.columnName：death_datetime
```

对应接口：

```text
GET /api/v1/factor-versions/101996817981379998
GET /api/v1/factor-versions/101996817981380000
GET /api/v1/compile-artifacts/101996817981380001
```

## 4. 因子按月试算

如页面允许对已发布因子版本发起试算，使用接口文档第 18.2 节的时间窗口。

试算时间：

```text
开始时间：2026-03-01T00:00:00
结束时间：2026-04-01T00:00:00
```

死亡患者数因子：

```text
因子版本 ID：101996817981379998
接口文档幂等键：mortality-v2-death-202603-fixed-20260727
接口文档批次 ID：101996817981380233
预期任务类型：FACTOR_TRIAL
预期批次状态：SUCCEEDED
预期质量状态：PASSED
预期结果：40.0
```

出院人次因子：

```text
因子版本 ID：101996817981380000
接口文档幂等键：mortality-v2-discharge-202603-fixed-20260727
接口文档批次 ID：101996817981380241
预期任务类型：FACTOR_TRIAL
预期批次状态：SUCCEEDED
预期质量状态：PASSED
预期结果：15647.0
```

页面预期：

```text
1. 发起试算后页面显示任务 ID 和批次 ID。
2. 查询结果后，死亡患者数显示 40.0 或等价值。
3. 查询结果后，出院人次显示 15647.0 或等价值。
4. 页面显示周期为 2026-03-01 至 2026-04-01。
```

对应接口：

```text
POST /api/v1/factor-versions/101996817981379998/trial
GET  /api/v1/factor-versions/101996817981379998/trials/101996817981380233/results?page=1&size=100
POST /api/v1/factor-versions/101996817981380000/trial
GET  /api/v1/factor-versions/101996817981380000/trials/101996817981380241/results?page=1&size=100
```

说明：

```text
页面如果使用新的幂等键重新发起试算，后端可能返回新的 taskId / batchId。
这种情况下，结果值仍应对齐 40.0、15647.0；批次 ID 可以不同。
```

## 5. 指标公式复测

接口文档第 18.3 节的指标对象：

```text
指标编码：MORTALITY_V2_RATE_FIXED_20260727_1412
指标 ID：101996817981380253
指标版本 ID：101996817981380254
```

进入页面：

```text
指标管理
```

查找：

```text
MORTALITY_V2_RATE_FIXED_20260727_1412
```

进入指标编辑或详情后，页面预期：

```text
1. 指标 ID 显示为 101996817981380253。
2. 指标版本 ID 显示为 101996817981380254。
3. 公式分子引用因子版本 101996817981379998。
4. 公式分母引用因子版本 101996817981380000。
5. 公式展示为死亡患者数 / 出院人次。
6. 展示格式为 PERCENT，multiplier 为 100，scale 为 2，roundingMode 为 HALF_UP。
```

接口文档公式 AST 参数：

```json
{
  "resourceVersion": 0,
  "formula": {
    "schemaVersion": "1.0",
    "astType": "INDICATOR_FORMULA",
    "root": {
      "nodeId": "mortality_div",
      "nodeType": "BINARY",
      "operator": "DIV",
      "left": {
        "nodeId": "death_factor",
        "nodeType": "FACTOR_REF",
        "factorVersionId": 101996817981379998
      },
      "right": {
        "nodeId": "discharge_factor",
        "nodeType": "FACTOR_REF",
        "factorVersionId": 101996817981380000
      },
      "zeroDenominatorPolicy": "RETURN_NULL"
    },
    "display": {
      "format": "PERCENT",
      "multiplier": "100",
      "scale": 2,
      "roundingMode": "HALF_UP"
    }
  }
}
```

对应接口：

```text
GET  /api/v1/indicators/101996817981380253
GET  /api/v1/indicators/101996817981380253/versions
GET  /api/v1/indicator-versions/101996817981380254
PUT  /api/v1/indicator-versions/101996817981380254/formula
POST /api/v1/indicator-versions/101996817981380254/formula/compile
```

如果页面需要重新保存并编译公式：

```text
保存公式时 resourceVersion 使用页面当前版本值。
接口文档实测保存后的下一步编译 resourceVersion 为 1。
编译成功后预期 artifactId 为 101996817981380257，status 为 VALID。
```

## 6. 指标试算

进入指标编辑页：

```text
公式与试算
```

试算时间必须使用接口文档第 18.3 节：

```text
开始时间：2026-03-01T00:00:00
结束时间：2026-04-01T00:00:00
接口文档幂等键：mortality-v2-indicator-fixed-202603-20260727
接口文档批次 ID：101996817981380260
```

页面预期：

```text
1. 发起试算后显示任务 ID 和批次 ID。
2. 任务类型为 INDICATOR_TRIAL。
3. 查询结果后 batchStatus 为 SUCCEEDED。
4. targetStatus 为 READY。
5. qualityStatus 为 PASSED。
6. resultValue 为 0.0025564。
7. displayValue 为 0.26%。
```

对应接口：

```text
POST /api/v1/indicator-versions/101996817981380254/trial
GET  /api/v1/indicator-versions/101996817981380254/trials/101996817981380260/results?page=1&size=100
```

说明：

```text
如果页面重新发起试算，后端可能产生新的 taskId / batchId。
新批次可以不同，但相同时间窗口下的 resultValue 和 displayValue 应与接口文档一致。
```

## 7. 指标发布与分析

接口文档第 18.4 节发布接口：

```text
POST /api/v1/indicator-versions/101996817981380254/publish
```

当前前端发布按钮的实际行为：

```text
用户点击“发布指标版本”后，前端按“缺哪步补哪步”的方式处理。
如果用户已经在“公式与试算”中完成“发起试算”和“查看结果”，点击发布时不会再次保存公式、编译、试算或读取结果。

仅当前置状态缺失时，前端才自动补齐：
1. 缺公式时，将公式引用切换为接口文档第 18 章实测通过的两个已发布因子版本：
   分子 101996817981379998，分母 101996817981380000。
2. 缺公式时，调用 GET /api/v1/indicator-versions/{id} 刷新最新 resourceVersion。
3. 缺公式时，调用 PUT /api/v1/indicator-versions/{id}/formula 保存公式。
4. 缺编译时，调用 POST /api/v1/indicator-versions/{id}/formula/compile 编译公式。
5. 缺试算批次时，调用 POST /api/v1/indicator-versions/{id}/trial 发起试算，默认周期为 2026-03-01T00:00:00 到 2026-04-01T00:00:00。
6. 缺试算结果时，调用 GET /api/v1/indicator-versions/{id}/trials/{batchId}/results 读取试算结果。
7. 前置状态具备后，调用 POST /api/v1/indicator-versions/{id}/publish，发布接口请求体仍为 {}。
```

页面可见检查点：

```text
1. 在“公式与试算”页签点击“发布指标版本”。
2. 如果此前已经看到试算结果 0.26% 或等价展示，页面“最近一次后端请求”应直接进入发布指标版本。
3. 如果此前未保存公式，自动保存公式后，页面中的分子因子应显示 101996817981379998。
4. 如果此前未保存公式，自动保存公式后，页面中的分母因子应显示 101996817981380000。
5. 如果此前未查看结果，自动查看结果成功后，页面应显示试算结果 0.26% 或等价展示。
6. 发布成功后，页面显示“指标版本已发布”或发布状态 PUBLISHED。
```

乐观锁处理说明：

```text
如果页面曾出现“指标版本已被修改，请刷新后重试”，这是 resourceVersion 乐观锁冲突。
当前前端已在保存公式前、编译前、发布前自动刷新 GET /api/v1/indicator-versions/{id}。
如果保存公式或发布时仍遇到一次 409 乐观锁错误，前端会自动刷新后重试一次。
用户只需观察页面最终是否显示发布成功；如果第二次仍失败，记录页面展示的 traceId。
```

服务内部错误处理说明：

```text
如果点击发布后页面提示“服务内部错误 (traceId: ...)”，说明请求已经到达后端，但后端内部执行失败。
这类错误不能通过 Network 面板作为验收依据，也不是前端页面可自行确认通过的状态。
页面验收时应记录 traceId，并以页面显示的错误提示作为失败证据，交由后端排查日志。
```

发布成功的页面预期：

```text
1. 发布状态显示 PUBLISHED。
2. 页面可见已发布版本 ID：101996817981380254。
3. 若页面展示发布返回信息，应能看到 backfillStartDate 为 2025-09-01。
4. 若页面展示回补信息，应能看到 monthlyBackfillBatchCount 为 10、yearlyBackfillBatchCount 为 1。
```

进入页面：

```text
指标分析
```

选择或定位：

```text
指标 ID：101996817981380253
指标版本 ID：101996817981380254
```

指定月份分析查询参数：

```text
indicatorVersionId=101996817981380254
periodStart=2026-03-01
periodEnd=2026-03-31
granularity=MONTHLY
```

页面预期：

```text
1. dataAvailable 为 true 或页面显示后端正式分析结果。
2. 当前值显示 0.26%。
3. overview.value 为 0.0025564 或页面等价展示。
4. overview.periodStart 为 2026-03-01T00:00:00。
5. overview.periodEnd 为 2026-04-01T00:00:00。
6. trend 至少包含 2026 年 3 月这一个点，displayValue 为 0.26%。
7. resultContext.batchId 显示为 101996817981380354。
```

对应接口：

```text
GET /api/v1/analysis/indicators/101996817981380253/analysis?indicatorVersionId=101996817981380254&periodStart=2026-03-01&periodEnd=2026-03-31&granularity=MONTHLY
```

## 8. 全历史趋势复测

接口文档第 18.5 节说明：看板趋势应省略 `periodStart` / `periodEnd`，读取全部已激活结果。

月度趋势查询：

```text
GET /api/v1/analysis/indicators/101996817981380253/analysis?indicatorVersionId=101996817981380254&granularity=MONTHLY
```

页面月度趋势至少应覆盖：

```text
2025-09：0.41%
2025-10：0.24%
2025-11：0.33%
2025-12：0.28%
2026-01：0.28%
2026-02：0.26%
2026-03：0.26%
2026-04：4.12%
2026-05：NOT_CALCULABLE 或空值
2026-06：NOT_CALCULABLE 或空值
```

年度趋势查询：

```text
GET /api/v1/analysis/indicators/101996817981380253/analysis?indicatorVersionId=101996817981380254&granularity=YEARLY
```

页面年度趋势预期：

```text
2025 年：0.52%
```

## 9. 若必须重新创建对象

严格复测应优先使用接口文档第 18 章已经存在的对象和 ID。只有在后端环境被清空、这些对象不存在时，才重新创建。

重新创建时必须遵守：

```text
1. 不复用接口文档中已存在的业务编码，否则可能 409 冲突。
2. 业务编码可以在接口文档编码后追加新的唯一后缀。
3. 字段、语义表、公式、时间窗口、display 配置必须保持接口文档参数语义。
4. 数据域必须仍绑定 vmq_deathpatientdetail 与 vmq_basicinformationba。
5. 默认时间语义字段必须仍分别映射为 DEATH_TIME 与 DISCHARGE_TIME。
6. 因子 DSL 应优先使用 filters: {"nodeType":"TRUE"}，依赖语义表默认时间字段自动绑定 period。
```

重新创建时允许变化的内容：

```text
数据域编码后缀
因子编码后缀
指标编码后缀
后端返回的新 ID
后端返回的新 taskId / batchId
```

重新创建时不应变化的内容：

```text
源表：vmq_deathpatientdetail、vmq_basicinformationba
默认时间语义字段：DEATH_TIME、DISCHARGE_TIME
公式结构：死亡患者数 / 出院人次
zeroDenominatorPolicy：RETURN_NULL
展示格式：PERCENT
multiplier：100
scale：2
roundingMode：HALF_UP
测试窗口：2026-03-01T00:00:00 至 2026-04-01T00:00:00
```

## 10. 最终验收标准

只看前端页面，满足以下现象即可认为本次“按接口文档第 18 章复测”通过：

```text
1. 数据管理页能看到 MORTALITY_V2_DEATH_20260727_1403。
2. 点击死亡数据域后能看到 DEATH_TIME。
3. 数据管理页能看到 MORTALITY_V2_DISCHARGE_20260727_1403。
4. 点击出院数据域后能看到 DISCHARGE_TIME。
5. 能定位死亡患者数因子版本 101996817981379998，状态为 PUBLISHED。
6. 能定位出院人次因子版本 101996817981380000，状态为 PUBLISHED。
7. 因子按 2026-03-01 至 2026-04-01 试算后，分子结果为 40.0。
8. 因子按 2026-03-01 至 2026-04-01 试算后，分母结果为 15647.0。
9. 能定位指标 MORTALITY_V2_RATE_FIXED_20260727_1412。
10. 指标版本为 101996817981380254。
11. 指标公式引用分子 101996817981379998 与分母 101996817981380000。
12. 指标公式编译状态为 VALID。
13. 指标试算 resultValue 为 0.0025564。
14. 指标试算 displayValue 为 0.26%。
15. 指标发布状态为 PUBLISHED。
16. 指标分析月度结果 dataAvailable 为 true，2026 年 3 月展示值为 0.26%。
17. 全历史月度趋势包含接口文档第 18.5 节列出的月度点。
18. 全历史年度趋势包含 2025 年 0.52%。
```

## 11. 第19章 费用类指标端到端验收

接口文档第 19 章"费用类指标完整链路实测：住院患者次均费用与抗菌药物费用占比"已完成后端实测。本节说明如何在当前前端页面中验证这些指标的分析展示。

### 11.1 测试对象与验证目标

| 对象 | 编码/ID | 验证目标 |
|------|---------|---------|
| 住院总费用因子版本 | 101996817981382014 | 已发布 PUBLISHED |
| 抗菌药物费用因子版本 | 101996817981382016 | 已发布 PUBLISHED |
| 出院人次因子版本 | 101996817981382018 | 已发布 PUBLISHED |
| 次均费用指标 | IND_AVG_INPATIENT_COST_20260727_1640 / 101996817981382232 | 趋势 + 科室对比 + CURRENCY 格式 |
| 抗菌药物占比指标 | IND_ANTIBACTERIAL_COST_RATIO_20260727_1640 / 101996817981382233 | 趋势 + 科室对比 + 百分比格式 |

### 11.2 因子列表验证

进入页面：

```
因子管理
```

页面预期：

```text
1. 因子列表中有"住院总费用"相关因子，版本状态为 PUBLISHED。
2. 因子列表中有"抗菌药物费用"相关因子，版本状态为 PUBLISHED。
3. 因子列表中有"出院人次"因子，版本状态为 PUBLISHED。
```

对应接口：

```text
GET /api/v1/factors
GET /api/v1/factor-versions
```

### 11.3 指标列表验证

进入页面：

```
指标管理
```

页面预期：

```text
1. 能找到编码为 IND_AVG_INPATIENT_COST_20260727_1640 的指标。
2. 能找到编码为 IND_ANTIBACTERIAL_COST_RATIO_20260727_1640 的指标。
```

对应接口：

```text
GET /api/v1/indicators
```

### 11.4 次均费用指标分析验收

进入页面：

```
指标分析
```

操作：

```text
1. 在指标下拉框中选择"住院患者次均费用"。
2. 停留在"趋势分析" Tab。
3. 切到"科室排名" Tab。
```

页面预期：

```text
趋势分析 Tab：
1. 后端趋势数据已接入，页面不再显示"本地演示数据"标签。
2. 2026-03 月全院平均展示值约为 ¥9695.26。

科室排名 Tab：
1. 不再显示"演示数据 · 2024 年度" StatusBadge，改为"后端维度数据"。
2. 表格包含 53 条科室记录（来自后端 dimensionComparison）。
3. 肝胆胰外科 约 ¥20551.83。
4. 妇科 约 ¥13655.44。
5. 数据来自后端 dimensionComparison，不再使用本地演示数据。
```

对应接口：

```text
GET /api/v1/analysis/indicators/101996817982382232/analysis?indicatorVersionId=101996817982382234&granularity=MONTHLY
```

说明：

```text
如果下拉框中看不到"住院患者次均费用"，说明 GET /api/v1/indicators 未返回第19章费用指标，
需确认后端环境是否包含这些指标（可能因数据库重置而丢失）。
```

### 11.5 抗菌药物费用占比指标验收

操作：

```text
在指标下拉框中选择"抗菌药物费用占比"。
切到"科室排名" Tab。
```

页面预期：

```text
1. 全院所有科室的抗菌药物费用占比均为 0.00%。
2. 这是源数据中 ANTIBACTERIAL_COST 字段全为 0 的实际计算结果，非错误或数据缺失。
3. 趋势图中所有月份均显示 0.00%。
```

对应接口：

```text
GET /api/v1/analysis/indicators/101996817982382233/analysis?indicatorVersionId=101996817982382235&granularity=MONTHLY
```

### 11.6 全历史趋势验证

操作：

```text
在指标下拉框中选择"住院患者次均费用"。
将粒度切换为"年度"。
```

页面预期：

```text
1. 年度趋势显示 2025 年数据，约为 ¥3350.54（年平均值）。
2. 月度趋势覆盖 2025-09 至 2026-04。
```

对应接口（不带 periodStart/periodEnd，读取全部已激活结果）：

```text
GET /api/v1/analysis/indicators/101996817982382232/analysis?indicatorVersionId=101996817982382234&granularity=MONTHLY
GET /api/v1/analysis/indicators/101996817982382232/analysis?indicatorVersionId=101996817982382234&granularity=YEARLY
```

### 11.7 第19章最终验收标准

只看前端页面，满足以下现象即可认为"第19章费用指标前端展示"通过：

```text
1. 因子管理列表中能看到已发布的费用类因子（总费用、抗菌药物费用、出院人次）。
2. 指标管理列表中能看到 IND_AVG_INPATIENT_COST_20260727_1640 和 IND_ANTIBACTERIAL_COST_RATIO_20260727_1640。
3. 指标分析页选择"住院患者次均费用"后，科室排名 Tab 不再显示"演示数据"标签。
4. 次均费用科室排名包含 53 条记录，金额约为 ¥20551.83（最高科室）至 ¥13655.44（妇科）等。
5. 指标分析页选择"抗菌药物费用占比"后，全院所有科室显示 0.00%。
6. Network 中能看到 GET /api/v1/analysis/indicators/{id}/analysis?indicatorVersionId=...&granularity=MONTHLY 请求。
```

### 11.8 注意事项

```text
1. 第19章费用指标与第18章住院死亡率指标共用同一数据域 INPATIENT_COST_20260727_1640。
   数据域绑定在源表 vmq_basicinformationba 上，源表字段 ANTIBACTERIAL_COST 全为 0 导致抗菌药物占比始终为 0。
2. 如果数据库被清空，第19章指标和因子 ID 会变化，需以当时后端返回的实际 ID 为准。
3. 前端科室排名使用后端 dimensionComparison 数据；当 dimensionComparison 为空数组时，
   降级使用 currentProfile.rankRows 本地演示数据。
4. CURRENCY 格式（次均费用）展示时，后端返回的 displayValue 已包含格式化字符串，
   前端直接渲染 displayValue 而不再次处理。
```

## 12. 从零创建三个因子与两个指标（完整链路）

本节说明如何通过前端页面依次创建三个因子，然后基于这三个因子创建两个指标。操作顺序为：

```
住院总费用因子 → 抗菌药物费用因子 → 出院人次因子 → 发布三个因子 → 次均费用指标 → 抗菌药物占比指标
```

### 12.1 创建因子一：住院总费用

#### 12.1.1 进入因子编辑页

```
路径：/factor/edit/new
入口：因子管理 → 右下角"新增因子"按钮
```

页面预期：

```
1. 页面标题显示"因子编辑 / 新增因子"。
2. 页面顶部显示数字编码（新建后变为因子名称）。
3. 基本信息表单已展开。
```

#### 12.1.2 填写基本信息

```
因子编码：保持页面自动生成，或手动输入如 FRONTEND_COST_TOTAL_{日期时间}
因子名称：住院总费用
说明：统计住院患者总费用，用于计算次均费用和费用占比指标。
```

点击填写后的表单预期：

```
因子编码输入框：有内容（自动生成或手动输入）
因子名称输入框：住院总费用
因子类型下拉框：原子因子（默认禁用）
说明文本框：统计住院患者总费用，用于计算次均费用和费用占比指标。
```

#### 12.1.3 配置计算口径

```
第一步：选择统计方式
点击"求和类因子"卡片（蓝色高亮表示选中）

第二步：选择数据来源
数据来源下拉框：选择包含费用字段的数据域（如 INPATIENT_COST_20260727_1640）
语义表：选择对应的语义表（如 INPATIENT_COST_TABLE_20260727_1640）
统计对象字段：TOTAL_COST（或对应的费用字段）

第三步：统计周期
选择"不限定统计周期"（单选按钮选中）

第四步：分组维度
不选择（保持为空，输出一个汇总值）
结果单位：元
```

点击后的页面状态：

```
求和类因子卡片：边框变蓝，背景变浅蓝
数据来源下拉框：显示选中的数据域名称
"当前口径"预览区：显示类似"从【INPATIENT_COST_20260727_1640】中...按【求和】统计【TOTAL_COST】...汇总。"
```

#### 12.1.4 保存并校验口径

```
操作：点击"保存并校验口径"按钮
位置：计算口径配置区底部右侧
```

点击后页面变化（等待 5-15 秒）：

```
1. 按钮显示 loading 状态（圆圈旋转）。
2. 保存成功后弹出消息："因子定义已保存"。
3. 弹出消息："计算口径校验通过"或显示校验状态。
4. "saved-summary" 区域显示：
   - 因子 ID：如 10199681798138XXXX
   - 版本 ID：如 10199681798138XXXX
   - 保存状态：DRAFT 或 VALID
```

#### 12.1.5 发起试算

```
试算时间：
开始时间：2026-03-01T00:00:00
结束时间：2026-04-01T00:00:00

操作：点击"发起试算"按钮
```

等待试算完成（可能需要 10-30 秒，取决于后端队列）：

```
页面预期：
1. 任务 ID 显示：如 10199681798138XXXX
2. 批次 ID 显示：如 10199681798138XXXX
3. 任务状态：从 RUNNING 变为 SUCCEEDED
4. 弹出消息："试算任务已提交"或"试算任务已完成"
```

#### 12.1.6 查看试算结果

```
操作：点击"查看试算结果"按钮
```

页面预期：

```
结果集状态：READY
最新结果：显示具体金额数值（如 8671201.27 或类似值）
表格显示：结果状态、结果值、显示值、质量状态
质量状态列：PASSED
```

#### 12.1.7 发布因子版本

```
操作：点击"发布因子版本"按钮
```

页面预期：

```
1. 发布状态从空变为 PUBLISHED。
2. 弹出消息："因子版本已发布"。
```

**保存以下信息供后续指标创建使用：**
```
因子版本 ID（版本 ID）：10199681798138XXXX（以后续实际返回为准）
```

---

### 12.2 创建因子二：抗菌药物费用

#### 12.2.1 进入新因子编辑页

```
操作：点击左上角"返回列表" → 因子管理 → 新增因子
路径：/factor/edit/new
```

#### 12.2.2 填写基本信息

```
因子名称：抗菌药物费用
说明：统计住院患者抗菌药物费用，用于计算抗菌药物费用占比指标。
```

#### 12.2.3 配置计算口径

```
第一步：选择统计方式
点击"求和类因子"卡片

第二步：选择数据来源
数据来源：与因子一相同的数据域
语义表：与因子一相同的语义表
统计对象字段：ANTIBACTERIAL_COST（或对应的抗菌药物费用字段）

第三步：统计周期
选择"不限定统计周期"

第四步：分组维度
不选择
结果单位：元
```

#### 12.2.4 保存、校验、试算、发布

```
操作顺序：
1. 点击"保存并校验口径" → 等待成功
2. 点击"发起试算" → 等待 RUNNING 变为 SUCCEEDED
3. 点击"查看试算结果" → 确认有结果值（预期为 0.0 或较小数值）
4. 点击"发布因子版本" → 确认 PUBLISHED
```

**保存以下信息：**
```
因子版本 ID：10199681798138XXXX（以后续实际返回为准）
```

---

### 12.3 创建因子三：出院人次

#### 12.3.1 进入新因子编辑页

```
操作：返回列表 → 因子管理 → 新增因子
路径：/factor/edit/new
```

#### 12.3.2 填写基本信息

```
因子名称：出院人次
说明：统计住院出院人次，用于计算次均费用和住院死亡率等指标。
```

#### 12.3.3 配置计算口径

```
第一步：选择统计方式
点击"计数类因子"卡片（计数类默认统计记录数）

第二步：选择数据来源
数据来源：INPATIENT_DISCHARGE_RECORD 或费用数据域下的出院语义表
语义表：选择对应的出院语义表
统计对象：不用选择（计数类默认统计记录数）

第三步：统计周期
选择"不限定统计周期"

第四步：分组维度
不选择
结果单位：人次
```

#### 12.3.4 保存、校验、试算、发布

```
操作顺序：
1. 点击"保存并校验口径" → 等待成功
2. 点击"发起试算" → 等待 SUCCEEDED
3. 点击"查看试算结果" → 确认有结果值（如 635 或类似数值）
4. 点击"发布因子版本" → 确认 PUBLISHED
```

**保存以下信息（用于后续指标创建）：**
```
因子版本 ID（出院人次）：10199681798138XXXX
因子版本 ID（住院总费用）：10199681798138XXXX（来自12.1节）
因子版本 ID（抗菌药物费用）：10199681798138XXXX（来自12.2节）
```

---

### 12.4 创建指标一：住院患者次均费用

#### 12.4.1 进入指标编辑页

```
路径：/indicator/edit/new
入口：指标管理 → 右下角"新增指标"按钮
```

#### 12.4.2 填写基本信息

```
指标编码：IND_AVG_INPATIENT_COST_{日期}
指标名称：住院患者次均费用
指标属性：定量
计量单位：元
指标导向：逐步降低 ↓（或监测比较）
统计周期：月度
```

#### 12.4.3 保存并创建版本

```
操作：点击"保存并创建版本"按钮
位置：基本信息表单底部右侧
```

点击后页面变化：

```
1. 按钮显示 loading。
2. 成功后弹出消息。
3. 版本 ID 显示：如 10199681798138XXXX
4. Tab 页签"2 公式与试算"变为可用。
```

#### 12.4.4 配置公式

```
操作：点击"2 公式与试算" Tab
```

在公式编辑器中：

```
1. 在左侧"可用因子"列表中找到"住院总费用"和"出院人次"。
2. 将"住院总费用"拖入分子（NUMERATOR）槽位。
3. 将"出院人次"拖入分母（DENOMINATOR）槽位。
4. 操作符自动显示为"÷"。
```

拖拽后页面状态：

```
分子槽位：显示"住院总费用"，下方有版本 ID
分母槽位：显示"出院人次"，下方有版本 ID
公式预览区：显示"指标值 = 住院总费用 ÷ 出院人次"
验证状态：显示绿色勾号"本地结构检查通过"
```

#### 12.4.5 保存公式

```
操作：点击"保存公式"按钮
```

点击后页面变化：

```
1. 按钮显示 loading。
2. 成功后弹出消息："公式已保存"。
3. 幂等键显示在调试区。
```

#### 12.4.6 公式校验

```
操作：点击"公式校验"按钮
```

点击后页面变化（等待 3-10 秒）：

```
1. 按钮显示 loading。
2. 成功后弹出消息："公式校验通过"或显示编译状态 VALID。
3. "发起试算"按钮变为可用（不再禁用）。
```

#### 12.4.7 发起试算

```
试算时间：
开始时间：2026-03-01T00:00:00
结束时间：2026-04-01T00:00:00

操作：点击"发起试算"按钮
```

等待试算完成（10-30 秒）：

```
页面预期：
1. 任务 ID 和批次 ID 显示。
2. 任务状态变为 SUCCEEDED。
3. "查看结果"按钮可用。
```

#### 12.4.8 查看结果

```
操作：点击"查看结果"按钮
```

页面预期：

```
试算结果区域显示：
- 批次 ID
- 结果值：如 13655.44（或类似计算后的数值）
- 展示值：¥13655.44（带货币符号和两位小数）
```

#### 12.4.9 发布指标版本

```
操作：点击"发布指标版本"按钮
```

页面预期：

```
1. 发布状态显示 PUBLISHED。
2. 弹出消息："指标版本已发布"。
```

**保存以下信息供指标分析验证：**
```
指标 ID：10199681798138XXXX
指标版本 ID：10199681798138XXXX
```

---

### 12.5 创建指标二：抗菌药物费用占比

#### 12.5.1 进入新指标编辑页

```
操作：返回指标管理 → 新增指标
路径：/indicator/edit/new
```

#### 12.5.2 填写基本信息

```
指标编码：IND_ANTIBACTERIAL_COST_RATIO_{日期}
指标名称：抗菌药物费用占比
指标属性：定量
计量单位：百分比（%）
指标导向：逐步降低 ↓
统计周期：月度
```

#### 12.5.3 保存并创建版本

```
操作：点击"保存并创建版本"按钮
```

#### 12.5.4 配置公式

```
操作：点击"2 公式与试算" Tab

1. 将"抗菌药物费用"拖入分子槽位。
2. 将"住院总费用"拖入分母槽位。
3. 操作符显示为"÷"。
4. 乘数自动显示为"× 100%"（表示百分比格式）。
```

#### 12.5.5 保存、校验、试算、发布

```
操作顺序：
1. 点击"保存公式" → 等待成功
2. 点击"公式校验" → 等待 VALID
3. 点击"发起试算" → 等待 SUCCEEDED
4. 点击"查看结果" → 确认结果（预期全为 0.00%，因为源数据 ANTIBACTERIAL_COST 为 0）
5. 点击"发布指标版本" → 确认 PUBLISHED
```

页面预期（查看结果）：

```
结果值：0.0
展示值：0.00%
```

这是正常现象，因为源数据中抗菌药物费用字段全为 0。

---

### 12.6 验证指标分析展示

#### 12.6.1 进入指标分析页

```
路径：/analysis
```

#### 12.6.2 验证次均费用指标

```
操作：
1. 在指标下拉框中选择"住院患者次均费用"。
2. 停留在"趋势分析" Tab。
3. 切到"科室排名" Tab。
```

页面预期：

```
趋势分析 Tab：
1. 不再显示"本地演示数据"标签。
2. 2026-03 月展示值约为 ¥9695.26。

科室排名 Tab：
1. 显示后端维度数据。
2. 包含多条科室记录。
3. 金额最高的科室约 ¥20551.83（肝胆胰外科）。
4. 妇科约 ¥13655.44。
```

#### 12.6.3 验证抗菌药物费用占比指标

```
操作：在指标下拉框中选择"抗菌药物费用占比"。
```

页面预期：

```
1. 全院所有科室的抗菌药物费用占比均为 0.00%。
2. 这是源数据计算结果，非错误。
3. 趋势图中所有月份均显示 0.00%。
```

---

### 12.7 最终验收标准

只看前端页面，满足以下现象即可认为"从零创建三个因子与两个指标"通过：

```
因子创建：
1. 住院总费用因子创建成功，状态为 PUBLISHED。
2. 抗菌药物费用因子创建成功，状态为 PUBLISHED。
3. 出院人次因子创建成功，状态为 PUBLISHED。

指标创建：
4. 住院患者次均费用指标创建成功，状态为 PUBLISHED。
5. 抗菌药物费用占比指标创建成功，状态为 PUBLISHED。

指标分析：
6. 选择次均费用指标后，科室排名显示后端数据。
7. 次均费用科室排名包含多条记录，金额带 ¥ 符号。
8. 选择抗菌药物费用占比后，全院显示 0.00%。

Network 验证：
9. 能看到 POST /api/v1/factors 请求。
10. 能看到 POST /api/v1/factor-versions/{id}/trial 请求。
11. 能看到 POST /api/v1/indicator-versions/{id}/formula 保存公式请求。
12. 能看到 POST /api/v1/indicators/{id}/versions/trial 请求。
13. 能看到 GET /api/v1/analysis/indicators/{id}/analysis 请求。
```

### 12.8 注意事项

```
1. 创建因子时，数据域必须是后端已存在且包含对应字段的数据域。
   如果数据域不存在，需先在"数据管理"页面确认数据域可用。

2. 因子试算时间窗口可以自行定义，但建议使用接口文档中的 2026-03-01 至 2026-04-01 以便对齐验证。

3. 抗菌药物费用因子试算结果为 0 是正常现象，因为源数据中该字段全为 0。
   这不影响指标创建链路的完整性验证。

4. 指标公式中的因子引用使用的是因子版本 ID，不是因子 ID。
   确保引用的因子版本状态为 PUBLISHED。

5. 如果试算任务一直显示 RUNNING，等待超过 60 秒后页面会提示"任务仍在运行中"。
   此时可以稍后再次点击"查看结果"或重新发起试算。

6. 页面中显示的 ID（因子 ID、版本 ID、批次 ID）以后端实际返回为准，
   接口文档中的 ID 仅供参考。
```
